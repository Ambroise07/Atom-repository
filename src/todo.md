# 14, may 2026:

1. search a method on the docs that get the index of an item and the coordinate in numpy array. -ok : use np.argwhere or np.where

2. search a method for delete an item in a numpy array. -ok : create a new array like that : 
    new_matrix = np.load(self.readonly.file_path, allow_pickle=True)
    new_matrix = new_matrix[new_matrix['project'] != name.encode('utf-8')]


3. write apis.EDIT.delete code -ok 

4. write apis.READONLY.neightboors code -ok 

5. write apis.READONLY.coords code -ok 

6. write apis.READONLY.search code -ok 


# 16 may 2026

1. check tools.get_projects_start -ok 

2. install the color for cmd -ok : use rich

3. push on git

